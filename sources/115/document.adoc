= MN 115: Internationalized semantic punctuation framework
:docnumber: 115
:edition: 1
:revdate: 2025-09-23
:copyright-year: 2025
:language: en
:title-main-en: Internationalized semantic punctuation framework
:doctype: standard
:status: published
:mn-document-class: ribose
:mn-output-extensions: xml,html,pdf,rxl
:local-cache-only:
:toclevels: 3

.Foreword
This document is part of the Metanorma specifications series that defines
requirements for internationalized document rendering, specifically addressing
punctuation usage across multiple languages and writing systems.

== Introduction

Metanorma is a semantic authoring system that supports multiple languages and
the publication of multi-lingual output.

A major component of Metanorma architecture is dedicated to providing presentation rendering
for the semantic content it encodes. This means that Metanorma often composes punctuated
text to  represent semantic text, and that punctuation needs to follow the norms of a target
language. Metanorma requires a semantic framework to capture the various types of punctuation
it will add to the text it composes, and to provide well-governed rendering of that punctuation
in the languages it supports.

This document defines a framework for semantic punctuation usage across multiple
languages and writing systems.

The document covers the following languages and writing systems:

* CJK (East Asian ideographic writing systems)
  * Traditional Chinese (Taiwan/Hong Kong conventions)
  * Simplified Chinese (Mainland China conventions)
  * Japanese (including vertical text layout)
  * Korean (Hangul-specific rules)
* Latin-script languages (English, Spanish, French)
* Cyrillic-script languages (Russian)

The document defines punctuation marks into primary usage categories based
on the semantic function of the punctuation symbols.

This document addresses critical challenges including:

Internationalization of auto-numbered elements::
Applying appropriate punctuation to document elements, such as: phrases, lists,
figures, tables and references use. Enabling definition of Label Auto-assignment
Definition Language profiles (LADL) (<<MN112>>).

Internationalization of bibliographic citations::
Applying appropriate punctuation to in-text citations and reference lists,
allowing for citation style definition and language-specific variations.

Automatic spacing::
Applying appropriate whitespace around punctuation marks based on
language-specific rules.

Rendering in vertical text layouts::
Proper punctuation positioning and transformation in vertical text modes.


== Scope

This document defines a framework for semantic punctuation usage across multiple
languages and writing systems. It also outlines how that framework is realised
in Metanorma implementation, and how its configuration can be updated.

Examples and discussion about specific writing systems are as of this writing limited
to CJK and Latin; but the document is intended to be generally applicable.

Mathematical symbols are out of scope of this document. Numeric punctuation is in scope,
but it is not discussed in detail, as it is handled in Metanorma by distinct mechanisms.

[NOTE]
--
Following linguistic practice, and to avoid confusion with quotations, individual
characters are cited in angle practice, and where necessary following by Unicode codepoint, and
preceded by Unicode name; e.g.

* `<，>`
* `<，>` (U+FF0C)
* FULLWIDTH COMMA `<，>` (U+FF0C).
--

[bibliography]
== Normative references

* [[[MN112,MN 112]]], MN 112: _Label Auto-assignment Definition Language (LADL) specification_
* [[[ISO10646,ISO/IEC 10646:2020]]]
* [[[Unicode15,Unicode 15.1]]] _Unicode_ Edition 15.1. https://www.unicode.org


== Terms and definitions

=== script

set of symbols representing a particular language

=== writing system

script, and the rules whereby it represents a particular language

=== locale

regional variant of a language and/or script, typically specific to a country

=== ideographic

written character that represents a semantic component of a language, rather than a sound in a language

=== CJK

Chinese, Japanese, Korean, and other writing systems based on Chinese or East Asian ideographs

=== punctuation mark

symbol used in written language to clarify meaning, indicate clausal structure, separate
elements, or convey syntactic relationships

=== usage category

functional classification of punctuation marks based on their primary purpose in
text structure

[example]
====
Sentence delimiters, pause indicators, grouping markers.
====

=== language variant

specific form or application of a punctuation mark that differs between
languages or writing systems

[example]
====
Period: `<.>` (Latin) vs `<。>` (CJK)
====

=== full-width character

character that occupies the width of an ideographic character, commonly used in
East Asian typography

=== half-width character

character that occupies standard Latin character width, used in Western
typography and mixed East Asian text

=== directionality

text arrangement of characters, including left-to-right (LTR), used in Latin and Cyrillic,
right-to-left (RTL), used in Hebrew and Arabic, horizontal (identical to LTR), usually used in CJK,
and vertical (top-to-bottom in columns arranged from right to left), traditionally used in CJK

=== spacing rule

specification for whitespace insertion around punctuation marks that varies by
language, locale, and context

=== phrase

meaningful grouping of words, that is smaller than a sentence

== Framework

=== General

This framework provides guidelines for the consistent application of punctuation
marks across different languages and writing systems. It aims to ensure that
punctuation usage aligns with semantic meaning and cultural conventions.

Punctuation profile:: A set of rules and mappings that define how punctuation
marks are applied in a specific language, locale or document context. Even in the same
language and locale, different publishers may require different punctuation styles.
+
[example]
====
In Japanese, the
https://www.bunka.go.jp/seisaku/bunkashingikai/kokugo/hokoku/pdf/93651301_01.pdf[「公用文作成の考え方」（建議）（付）「公用文作成の考え方（文化審議会建議）」解説] of
the Agency for Cultural Affairs (2022) specifies different punctuation rules
from JIS X 8301:2019, the Japanese Industrial Standard stating requirements for
punctuation in Japanese standards

* sentence pausal mark: the former requires use of the _ten_ (IDEOGRAPHIC COMMA) `<、>` (U+3001),
 but the latter requires the use of the full-width Latin comma, FULLWIDTH COMMA `<，>` (U+FF0C).
====

Punctuation mark:: A symbol used in writing to clarify meaning, indicate phrase structure,
separate elements, or convey syntactic relationships. Each punctuation mark can have
several semantic functions, and these may vary across languages. Not all marks
exist in all languages. A mark is commonly represented by one or more glyphs.

Semantic function:: The purpose or role of a punctuation mark in text,
which may include indicating sentence and phrase boundaries, grouping, emphasis,
indicating relationships, semantic indication such as for named elements. The correspondence
of semantic function to punctuation mark is many-to-many.
+
[example]
====
The semantic function of "phrase delimiter within a sentence" is represented by
different punctuation marks in different languages:

* Japanese: IDEOGRAPHIC COMMA `<、>` (U+3001)
* Chinese: FULLWIDTH COMMA `<，>` (U+FF0C)
* English: COMMA `<,>` (U+002C)
====
+
Not all semantic functions exist in all languages.
+
[example]
The semantic function of "enumeration delimiter" used in
Traditional Chinese and Simplified Chinese does not exist in English or
Japanese.
+
The correspondence
of semantic function to punctuation mark is many-to-many.
+
[example]
====
In English, a period can be used as a sentence delimiter, but also as an abbreviation
marker (_Bros._, _p.m._). So English period is a punctuation mark fulfilling two semantic
functions. In Hebrew, there are two abbreviation markers, 
_geresh_ `<׳>` (U+05F3) for a single word (ר׳ "r." = רבי rabbi), and _gershayim_ `<״>` (U+05F4) 
for multi-word phrases (ארה״ב "U.S." =  ארצות הברית "United States"); these are
distinct from the sentence delimiter:  
ר׳  יעקב גר בארה״ב. וגם אני, "R(abbi) Yaakov lives in the U.S. And so do I".
So the mapping of abbreviation marker function is conflated with the sentence delimiter 
function as the period punctuation mark in English, but is split into a single-word and 
a multi-word punctuation mark in Hebrew. 

The _geresh_ also has other semantic functions, such as indicating
numerals, or as a diacritic for non-Hebrew sounds (גם /ɡam/ "also" vs ג׳ם /dʒam/ "jam"). 
As a diacritic, the _geresh_ is no longer punctuation, but represents phonological sounds.
====

Punctuation rule:: A guideline that specifies how a punctuation mark should be
used in a particular language or context, including placement, spacing, and
variations. Some punctuation rules are completely predictable, and can be automated. Other punctuation rules are not predictable, and need to be
applied manually
+
[example]
The semantic function of range in Chinese differentiates between word ranges, which use en-dash `<—>` (U+2013)
(1月—7月 "January to July", literally "Month 1–Month 7"), and numeric ranges, which use
wavy dash `<～>` (U+301C) (5～20個字 "5 to 20 words"). This punctuation rule is predictable: wavy dash is used between
two numerals (including Chinese numerals).
+
[example]
In Hebrew, _geresh_ follows the initial letters of a word as an abbreviation mark in Hebrew, but 
the abbreviation may involve just the first letter (ר׳ "r." = רבי "rabbi"), or more than one letter
(גב׳ "Mrs, Ms" = גברת "lady"). It is not predictable how many letters appear in a Hebrew abbreviation:
implementing Hebrew abbreviation on full words, in a semantic punctuation model, involves either
a lookup table of abbreviations, or providing the already abbreviated words as input. (The latter is
what is already done by typing גב׳, just as it is in English by typing _Mrs_ instead of `abbreviate("Mistress")`.)

A punctuation profile is defined by:

* A set of semantic functions that apply to semantic elements in text
* Punctuation marks that are associated with each semantic function
* A collection of predictable punctuation rules that govern their usage

Given a punctuation profile, a document rendering with appropriate punctuation
that suits the target audience can be achieved—provided that the needed punctuation
rules can be automated.


=== Punctuation semantic functions

Punctuation marks are classified into semantic function categories by their
primary functional usage rather than their linguistic origin.

This approach enables consistent implementation across different languages while
accommodating language-specific variations within each functional category.

Under each category of semantic functions, each distinct function in the framework is given,
along with its typical corresponding mark in English (or a different language, if its function
is conflated in the English punctuation mark.)

Each usage category and each individual function defines one of more of the following:

* Primary function and purpose
* Range of semantic functions they cover
* Common punctuation mark variations across languages for each function
* Spacing requirements
* Special handling rules

== Functions: phrase-level structure

=== Sentence stops

==== General

===== Primary function and purpose

Sentence stops delimit a full grammatical sentence. 

===== Range of semantic functions

Writing systems vary as to whether they also
terminate a standalone sentence, and therefore act as a sentence terminator.
That is the case in formal English (though often not in texting),
but it is not the case in Japanese.

===== Punctuation mark variations

* Korean sentence stops are half-width.
* Chinese and Japanese sentence stops are full-width. 
* In text mixed between Latin and Chinese or Japanese, usual practice is to follow the document
main language: e.g. _现在很多人都在用 iPhone。_, with full-width punctuation in a Chinese text after English words;
_This app 很好用._ in English text. Fine typography will apply kerning in such contexts so that the switch in
width does not look obtrusive. 
* Some desktop publishing applications in Japanese and CJK OpenType fonts allow switching contextually 
between full-width and half-width punctuation (欧文混在時の約物処理 "treat punctuation when mixed with Western text"). 

[[sentence-stop-spacing]]
===== Spacing rules 

* Latin and Cyrillic require space after sentence stops as delimiters between sentences, as does Korean.
* Writing systems do not use space after the final sentence stop in a paragraph.
* Chinese and Japanese by default do not use space after sentence stops. 
* None of these writing systems has space before sentence stops. 
* In French in France, Switzerland and Belgium, some punctuation marks, including
some sentence stops,
are preceded by a non-breaking thin space (U+202F) ("French spacing").
In common practice, a full non-breaking space (U+00A0) is used instead.
French spacing is not used for most punctuation marks in Canada for French.

==== Declarative stop (_period_)

===== Primary function and purpose

Mark that indicates the end of a complete declarative grammatical statement.
This function is represented in Metanorma i18n files as `punct.period`.




[example]
English: the period in _This is a sentence._

[example]
Traditional Chinese: the ideographic period in 這是一句句子。

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: FULL STOP `<.>` (U+002E)
* Traditional Chinese: IDEOGRAPHIC FULL STOP `<。>` (U+3002)
* Simplified Chinese: IDEOGRAPHIC FULL STOP `<。>` (U+3002)
* Japanese: IDEOGRAPHIC FULL STOP `<。>` (U+3002)
* Korean: FULL STOP `<.>` (U+002E)

////
[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|period
|`.`
|This is a sentence.

|Traditional Chinese
|句號
|`。`
|這是一句句子。

|Simplified Chinese
|句号
|`。`
|这是一句句子。

|Japanese
|句点
|`。`
|これは文です。

|Korean
|마침표
|`.`
|이것은 문장입니다.

|===
////

===== Special handling

The placement of period in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is a PRESENTATION FORM FOR VERTICAL IDEOGRAPHIC FULL STOP `<︒>` (U+FE12), this is only
included in Unicode as a compatibility character.)

* In Japanese and Simplified Chinese horizontal text, the period appears at the bottom right. 
In Japanese and Simplified Chinese vertical text, 
the period appears below and to the right of the character.
* In Traditional Chinese, the period appears at mid-height in both horizontal and vertical text orientations.



==== Interrogative stop (_question mark_)

===== Primary function and purpose

Mark that indicates this grammatical statement is a question.
This function is represented in Metanorma i18n files as `punct.question-mark`.

[example]
English: the question mark in _Is this a question?_

[example]
Traditional Chinese: the ideographic question mark in 這是問題嗎？

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: QUESTION MARK `<?>` (U+003F)
* Traditional Chinese: FULLWIDTH QUESTION MARK `<？>` (U+FF1F)
* Simplified Chinese: FULLWIDTH QUESTION MARK `<？>` (U+FF1F)
* Japanese: FULLWIDTH QUESTION MARK `<？>` (U+FF1F)
* Korean: QUESTION MARK `<?>` (U+003F)


////
[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|question mark
|`?`
|Is this a question?

|Traditional Chinese
|問號
|`？`
|這是問題嗎？

|Simplified Chinese
|问号
|`？`
|这是问题吗？

|Japanese
|疑問符
|`？`
|これは質問ですか？

|Korean
|물음표
|`?`
|이것은 질문입니까?

|===
////

===== Spacing rules

* French spacing applies (<<sentence-stop-spacing>>).


===== Special handling
 
* In Spanish and languages under Spanish cultural influence (e.g. Catalan), INVERTED QUESTION MARK `<¿>` 
 (U+00BF) is used at the start of an interrogative sentence or phrase. If a declarative sentence contains
 an  interrogative phrase, only the interrogative phrase is so delimited: 
 _Si no puedes ir con ellos, ¿quieres ir con nosotros?_ ("If you cannot go with them, ¿would you like to go with us?")
* There is some variation by locale in usage: short questions can omit the inverted question mark
in Galician; Catalan in Catalonia does not use inverted question marks; Catalan in Valencia uses them
optionally.

TIP: Metanorma does not currently implement inverted question marks.


==== Exclamatory stop (_exclamation mark_)

===== Primary function and purpose

Mark that indicates this grammatical statement is an exclamation.
This function is represented in Metanorma i18n files as `punct.exclamation-mark`.


[example]
English: the exclamation mark in _What a great day!_

[example]
Traditional Chinese: the ideographic exclamation mark in 多麼美好的一天！

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: EXCLAMATION MARK `<!>` (U+0021)
* Traditional Chinese: FULLWIDTH EXCLAMATION MARK `<！>` (U+FF01)
* Simplified Chinese: FULLWIDTH EXCLAMATION MARK `<！>` (U+FF01)
* Japanese: FULLWIDTH EXCLAMATION MARK `<！>` (U+FF01)
* Korean: EXCLAMATION MARK `<!>` (U+0021)

////
[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|exclamation mark
|`!`
|What a great day!

|Traditional Chinese
|感嘆號
|`！`
|多麼美好的一天！

|Simplified Chinese
|感叹号
|`！`
|多么美好的一天！

|Japanese
|感嘆符
|`！`
|なんて素晴らしい日だ！

|Korean
|느낌표
|`!`
|정말 좋은 날이네요!

|===
////

===== Spacing rules

* French spacing applies (<<sentence-stop-spacing>>).

===== Special handling
 
* In Spanish and languages under Spanish cultural influence (e.g. Catalan), INVERTED EXCLAMATION MARK `<¡>` 
 (U+00A1) is used at the start of an exclamatory sentence or phrase. 
* The same locale considerations apply for Catalan as for inverted question mark.

TIP: Metanorma does not currently implement inverted exclamation marks.


=== Phrase stops

==== General

Phrase stops delimit phrases within a sentence.


==== Minor phrase separator (_comma_)

===== Primary function and purpose

Mark that delimits a minor break between phrases within a sentence.
This function is represented in Metanorma i18n files as `punct.comma`.

[example]
English: the comma in _This is a sentence, with a pause._

[example]
Traditional Chinese: the full-width comma in 這是一句句子，有停頓。

===== Range of semantic functions

* The contexts in which the minor break is marked between phrases varies significantly
by language and by style.
* The types of phrase eligible to be so marked also varies significantly
by language and by style; it usually includes both clauses (which express a complete predicate,
with both a subject and a verb) and smaller units such as noun phrases, or successive adjectives.
+
====
_Ich weiß, dass du lügst_ "I know, that you're lying", with comma separating a complement
clause from the main clause, is correct punctuation in German, but not English
====
* In many writing systems, the same mark (comma) is used for minor phrase separators and 
other separating functions, such as listing separators, and decimal points.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: COMMA `<,>` (U+002C)
* Traditional Chinese: FULLWIDTH COMMA `<，>` (U+FF0C)
* Simplified Chinese: FULLWIDTH COMMA `<，>` (U+FF0C)
* Japanese: IDEOGRAPHIC COMMA `<、>` (U+3001)
** In mixed Japanese-Western text, FULLWIDTH COMMA `<，>` (U+FF0C) can be used instead to maintain visual consistency
** Space can be used in Japanese corresponding to English comma or colon usage in certain contexts.
* Korean: COMMA `<,>` (U+002C)

===== Special handling

The placement of comma in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is a PRESENTATION FORM FOR VERTICAL COMMA `<︐>` (U+FE10) and a 
PRESENTATION FORM FOR VERTICAL IDEOGRAPHIC COMMA `<︑>` (U+FE11), these are only
included in Unicode as a compatibility character.)

* In Japanese and Simplified Chinese horizontal text, the comma appears at the bottom right. 
In Japanese and Simplified Chinese vertical text, 
the comma appears below and to the right of the character.
* In Traditional Chinese, the comma appears at mid-height in both horizontal and vertical text orientations.


////
[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|comma
|`,`
|This is a sentence, with a pause.

|Traditional Chinese
|逗號
|`，`
|這是一句句子，有停頓。

|Simplified Chinese
|逗号
|`，`
|这是一句句子，有停顿。

|Japanese
|読点
|`，`
|これは文です，停止があります。

|Korean
|쉼표
|`,`
|이것은 문장입니다, 일시정지가 있습니다.

|===
////




==== Major phrase separator (_semicolon_)

===== Primary function and purpose

Mark that delimits a major break between phrases within a sentence.
This function is represented in Metanorma i18n files as `punct.semicolon`.

[example]
English: the semicolon in _This is a sentence; with a pause._

[example]
Traditional Chinese: the full-width comma in 這是一句句子，有停頓。

===== Range of semantic functions

* The contexts in which the major break is marked between phrases varies significantly
by language and by style. In many styles, it is avoided as an overly fine distinction from the 
minor break (comma).
* The phrases separated by a major phrase separator are typically independent clauses grammatically
(i.e. complete predicates, with both a subject and a verb).

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: SEMICOLON `<;>` (U+002C)
* Traditional Chinese: FULLWIDTH SEMICOLON `<；>` (U+FF1B)
* Simplified Chinese: FULLWIDTH SEMICOLON `<；>` (U+FF1B)
* Japanese: FULLWIDTH SEMICOLON `<；>` (U+FF1B)
* Korean: SEMICOLON `<;>` (U+002C)

===== Spacing rules

* French spacing applies (<<sentence-stop-spacing>>).


===== Special handling

The placement of semicolon in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is a PRESENTATION FORM FOR VERTICAL SEMICOLON `<︔ >` (U+FE14), this is only
included in Unicode as a compatibility character.)


==== Introductory phrase separator (_colon_)

===== Primary function and purpose

Mark that delimits phrases within a sentence, where the first phrase
introduces the second.
This function is represented in Metanorma i18n files as `punct.colon`.

===== Range of semantic functions

* Occasionally punctuation is used to separate a term from a description in a definition list,
although the default is to use only indented space. This function can be regarded as an extension
of the introductory phrase separator. 
In Metanorma Presentation XML, this is notated as `<span class="fmt-dt-delim">`.

====
Framework:: basic structure underlying a system, concept, or text

Framework: basic structure underlying a system, concept, or text
====

===== Punctuation mark in scripts, languages, and locales

* Latin: COLON `<:>` (U+003A)
* Cyrillic: COLON `<,>` (U+003A)
* Traditional Chinese: FULLWIDTH COLON `<：>` (U+FF1A)
* Simplified Chinese: FULLWIDTH COLON `<：>` (U+FF1A)
* Japanese: FULLWIDTH COLON `<：>` (U+FF1B)
** Space can be used in Japanese corresponding to English comma or colon usage in certain contexts.
* Korean: COLON `<,>` (U+003A)

====== Spacing rules

* In French, colon is preceded by a non-breaking space. There is more variation
by locale of this spacing rule than for other punctuation marks, for which
French spacing applies (<<sentence-stop-spacing>>). 
** In Switzerland,
it is thin space (U+202F). 
** In Canada, colon is the only punctuation mark where French spacing 
is expected, and it is a full space (U+00A0). 
** In France, a full space (U+00A0) is usual.



===== Special handling

The placement of colon in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is a PRESENTATION FORM FOR VERTICAL COLON `<︓ >` (U+FE13), this is only
included in Unicode as a compatibility character.)

The interpunct is used instead of hyphen, dash, or colon in Japanese vertical text.


==== Breaking phrase separator (_em-dash_)

===== Primary function and purpose

Mark that delimits phrases within a sentence, where there is a conceptual break of some short
between the first phrase and the second.
This function is represented in Metanorma i18n files as `punct.em-dash`.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: EM DASH `<—>` (U+2014)
** The EN DASH `<–>` (U+2013) is also used in this function
* Traditional Chinese: TWO EM DASH `<⸺>` (U+2E3A)
* Simplified Chinese: TWO EM DASH `<⸺>` (U+2E3A)
* Japanese: TWO EM DASH `<⸺>` (U+2E3A)
** Informally in CJK, twice em dash (U+2014 U+2014) is used instead of the two em dash.
* Korean:  EM DASH `<—>` (U+2014)

====== Spacing rules

* There is variation between languages and locales as to whether em dash or en dash
is used. Spaces surrounding the dash are required for en dash, and may or may not be required
for em dash. The spaces are thin spaces in fine typography, but may be normal spaces in common practice.

===== Special handling

The placement of colon in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is a PRESENTATION FORM FOR VERTICAL EM DASH `<︱>` (U+FE31), this is only
included in Unicode as a compatibility character.)

The interpunct is used instead of hyphen, dash, or colon in Japanese vertical text.


=== Listing stops

==== General

===== Primary function and purpose

Listing stops are used to delimit multiple items in a listing, other than phrases. 


===== Range of semantic functions

Because of the conceptual similarity between phrase stops and listing stops, as joining multiple
items, punctuation marks for the two are often conflated. In fact, if the definition of "phrase"
is liberal enough, listing stops are phrase stops. This account takes the approach that punctuation
separating noun phrases is considered listing stops, and not phrase stops.

====
_I went to the market, and I bought some beans_: comma as phrase stop (it is separating two phrases that
are complete sentences)
_I bought some beans, cheese, and a nice bottle of Chianti_: comma as listing stop (it is separating noun phrases,
which can include articles, adjectives, and quantifiers)
====

==== Enumeration delimiter (_enumeration comma_)

===== Primary function and purpose

The enumeration delimiter separates items in a list.
This function is represented in Metanorma i18n files as `punct.enum-comma`,
and in Metanorma Presentation XML as `<span class="fmt-enum-comma">`.

In CJK, both comma-like punctuation marks and interpuncts are used as enumeration delimiters.
There is no consistent semantic distinction between the two, so no such distinction is made here.

[example]
Japanese: 小・中学校 or  小、中学校 "elementary, [and] middle school"


===== Range of semantic functions

* There is variation in whether the enumeration delimiter is used in a list of two items.
* There is (linguistic) variation in whether a conjunction is used before the last item in a list, 
and (punctuation) variation in whether the enumeration delimiter is used before the last item.
Thus English allows both _A, B and C_ and _A, B, and C_ as style variants (the latter is known as the
"Oxford comma"). Chinese allows both  _A、B及C_ and _A、B、C_.
* The punctuation mark for the enumeration delimiter is often the same as that for the minor clause stop (comma).
* The punctuation mark for the enumeration delimiter is often the same as that for other separators;
for example Japanese uses the interpunct as an enumeration delimiter, as a decimal point, to indicate
alternation between abbreviations, and to separate professional titles, names, and positions.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: COMMA `<,>` (U+002C)
* Traditional Chinese: IDEOGRAPHIC COMMA `<，>` (U+3001)
* Simplified Chinese: IDEOGRAPHIC COMMA `<，>` (U+3001)
* Japanese: IDEOGRAPHIC COMMA `<、>` (U+3001)
** Japanese uses the interpunct for short lists instead of comma in some contexts. This is rendered as
 KATAKANA MIDDLE DOT  `<・>` (U+30FB)
** In mixed Japanese-Western text, FULLWIDTH COMMA `<，>` (U+FF0C) can be used instead to maintain visual consistency
* Korean: COMMA `<,>` (U+002C)
** Korean uses the interpunct for short lists instead of comma in some contexts. This is rendered as
   HANGUL LETTER ARAEA `<ㆍ>` U+318D

===== Special handling

* When lists contain mixed scripts in CJK, follow the punctuation convention of the list's primary language 
while maintaining readability.
* Japanese also supports HALFWIDTH KATAKANA MIDDLE DOT `<･>` (U+FF65) for the interpunct.
* In Japanese vertical text, the interpunct appears centered rather than at a specific corner position.

////
[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|middle dot
|`·`
|First item · Second item · Third item

|Traditional Chinese
|間隔號
|`·`
|第一項·第二項·第三項

|Simplified Chinese
|间隔号
|`·`
|第一项·第二项·第三项

|Japanese
|中黒
|`・`
|第一項・第二項・第三項

|Korean
|가운뎃점
|`·`
|첫 번째 항목·두 번째 항목·세 번째 항목

|===
////

=== Quotation markers

==== General


===== Primary function and purpose

Quotation markers indicate that a span of text constitutes direct speech, or otherwise
attribute it to  some third party. 


===== Spacing rules

* In French in France, Switzerland and Belgium, for paired quotation delimiters,
the opening delimiter (left guillemet, left single guillemet) 
is followed by a non-breaking thin space (U+202F), 
as an instance of French spacing (<<sentence-stop-spacing>>); the closing delimiter
(right guillemet, right single guillemet) 
is preceded by a non-breaking thin space, as the mirror counterpart of French spacing.
As with other French spacing, in common practice, a full non-breaking space (U+00A0) 
is used instead.


==== Paired quotation delimiters (_double quotes_)
===== Primary function and purpose
Paired quotation delimiters indicate the start and end of quoted text.
This function is represented in Metanorma i18n files as `punct.open-quote` and `punct.close-quote`.

===== Range of semantic functions

* Paired quotation markers are often conflated with title markers.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: There is significant variation among Latin and Cyrillic script languages on their
choice of paired quotation delimiter, and different languages and locales draw from 15 Unicode codepoints.
** In American English, good typographical practice uses LEFT DOUBLE QUOTATION MARK `<“>` (U+201C) and 
  RIGHT DOUBLE QUOTATION MARK `<”>` (U+201D). In British English, some publishers use double quotation marks,
  and others use single quotation marks, LEFT SINGLE QUOTATION MARK `<‘>` (U+2018) and 
  RIGHT SINGLE QUOTATION MARK `<’>` (U+2019)
** French uses guillemets, LEFT-POINTING DOUBLE ANGLE QUOTATION MARK `<«>` (U+00AB) and
  RIGHT-POINTING DOUBLE ANGLE QUOTATION MARK `<»>` (U+00AB)
* Traditional Chinese: LEFT CORNER BRACKET `<「>` (U+300C) and RIGHT CORNER BRACKET `<」>` (U+300D)
* Simplified Chinese: LEFT DOUBLE QUOTATION MARK `<“>` (U+201C) and 
  RIGHT DOUBLE QUOTATION MARK `<”>` (U+201D). 
** The corner brackets of Traditional Chinese are also in common use.
* Japanese: LEFT CORNER BRACKET `<「>` (U+300C) and RIGHT CORNER BRACKET `<」>` (U+300D)
** Japanese also uses lenticular brackets, LEFT BLACK LENTICULAR BRACKET `<【>` (U+3010) and
  RIGHT BLACK LENTICULAR BRACKET `< 】>` (U+3011)
* Korean: South Korea uses the same punctuation marks as English. North Korea uses full-width guillemets,
LEFT DOUBLE ANGLE BRACKET `<《>` (U+300A) and RIGHT DOUBLE ANGLE BRACKET `<》>` (U+300B)
** Corner brackets and white corner brackets are often used in practice.




===== Special handling

* The placement of quotation marks in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is e.g. a PRESENTATION FORM FOR VERTICAL LEFT CORNER BRACKET `<﹁>` (U+FE41) and
PRESENTATION FORM FOR VERTICAL RIGHT CORNER BRACKET `<﹂>` (U+FE42), these are only
included in Unicode as a compatibility character.)
* Quotation marks in English are routinely typed as straight quotes, QUOTATION MARK `<">` (U+0022),
with the expectation that software will transform them contextually into paired quotation marks
("smart quotes").
+
--
TIP: Metanorma transforms straight quotes in Asciidoc source into smart quotes following English conventions.
--
* Simplified Chinese uses the font to make Western quotation marks (U+201C, U+201D) full-width, instead of deploying
distinct codepoints (REVERSED DOUBLE PRIME QUOTATION MARK `<〝>` (U+301D)
DOUBLE PRIME QUOTATION MARK `<〞>` (U+301E))
* In vertical directionality, Traditional and Simplified Chinese use presentation variants of the codepoints
LEFT WHITE CORNER BRACKET `<『>` (U+300E) and RIGHT WHITE CORNER BRACKET `< 』>` (U+300F).



==== Nested paired quotation delimiters (_single quotes_)

===== Primary function and purpose
Nested paired quotation delimiters indicate the start and end of quoted text within another
delimited quoted text, and attributed to a different speaker from the of the nesting quoted text.
This function is represented in Metanorma i18n files as `punct.open-nested-quote` and `punct.close-nested-quote`.

[example]
“Didn't she say ‘I like red best’ when I asked her wine preferences?” he asked his guests.

===== Range of semantic functions

* The English nested paired quotation delimiters are often conflated with the apostrophe.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: There is significant variation among Latin and Cyrillic script languages on their
choice of paired quotation delimiter, and different languages and locales draw from 15 Unicode codepoints.
** In American English, good typographical practice uses LEFT SINGLE QUOTATION MARK `<‘>` (U+2018) and 
  RIGHT SINGLE QUOTATION MARK `<’>` (U+2019). In British English, some publishers use single quotation marks,
  and others use double quotation marks, LEFT DOUBLE QUOTATION MARK `<“>` (U+201C) and 
  RIGHT DOUBLE QUOTATION MARK `<”>` (U+201D)
** French uses single guillemets, LEFT-POINTING SINGLE ANGLE QUOTATION MARK `<«>` (U+2039) and
  RIGHT-POINTING SINGLE ANGLE QUOTATION MARK `<»>` (U+203A)
* Traditional Chinese: LEFT WHITE CORNER BRACKET `<『>` (U+300E) and RIGHT WHITE CORNER BRACKET `<』>` (U+300F)
* Simplified Chinese: LEFT SINGLE QUOTATION MARK `<‘>` (U+2018) and 
  RIGHT SINGLE QUOTATION MARK `<’>` (U+2019). 
** The white corner brackets of Traditional Chinese are also in common use.
* Japanese: LEFT CORNER BRACKET `<「>` (U+300C) and RIGHT CORNER BRACKET `<」>` (U+300D)
** Japanese also uses lenticular brackets, LEFT BLACK LENTICULAR BRACKET `<【>` (U+3010) and
  RIGHT BLACK LENTICULAR BRACKET `< 】>` (U+3011)
* Korean: South Korea uses the same punctuation marks as English. North Korea uses full-width guillemets,
LEFT DOUBLE ANGLE BRACKET `<《>` (U+300A) and RIGHT DOUBLE ANGLE BRACKET `<》>` (U+300B)
** Corner brackets and white corner brackets are often used in practice.



===== Special handling

* The placement of quotation marks in CJK is different by directionality, but this does not involve 
different Unicode codepoints, so the browser/operating system is assumed to handle positioning appropriately.
(While there is e.g. a PRESENTATION FORM FOR VERTICAL LEFT WHITE CORNER BRACKET `<﹃>` (U+FE43) and
PRESENTATION FORM FOR VERTICAL RIGHT WHITE CORNER BRACKET `<﹄>` (U+FE44), these are only
included in Unicode as a compatibility character.)
* Nested quotation marks in English are routinely typed as straight quotes, APOSTROPHE `<'>` (U+0027),
with the expectation that software will transform them contextually into paired quotation marks
("smart quotes").
+
--
TIP: Metanorma transforms straight quotes in Asciidoc source into smart quotes following English conventions.
--
* Simplified Chinese uses the font to make Western quotation marks (U+2018, U+2019) full-width, instead of deploying
distinct codepoints
* In vertical directionality, Traditional and Simplified Chinese use presentation variants of the codepoints
LEFT CORNER BRACKET `<「>` (U+300C) and RIGHT CORNER BRACKET `<」>` (U+300D).

==== Single quotation delimiters (_quotation dash_)

===== Primary function and purpose

A single initial marker used to represent one turn in an alternation of direct speech.

[example]
====
― O saints above! Miss Douce said, sighed above her jumping rose. I wished I hadn't laughed so much. I feel all wet.

― O Miss Douce! Miss Kennedy protested. You horrid thing!
====

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: Properly HORIZONTAL BAR `<―>` (U+2015) is used; in practice EM DASH `<—>` (U+2014)
is usually used.
  * The quotation dash is almost unknown in English, but is commonplace in other European languages,
  such as French.

===== Spacing rules

There is a thin space after the quotation dash.



// Direct quotation:::
// Marks that enclose directly quoted speech or text.

// [cols="2,2,2,2,2,2"]
// |===
// |Function |English |Traditional Chinese |Simplified Chinese |Japanese |Korean

// |Direct quotation +
// 引號/引号/引用符/따옴표
// |"..."'...' +
// (quotation marks)
// |「...」『...』 +
// (引號)
// |"..."'...' +
// (引号)
// |「...」『...』 +
// (引用符)
// |"..."'...' +
// (따옴표)






== Functions: word-level structure

=== Conjunctors

==== General

Connection indicators link related elements, show relationships between parts,
or indicate continuation across boundaries.

==== Range mark (_en-dash_)

===== Primary function and purpose

A marker between two items, used to convey a range between the two. The items can be verbal values (_Paris–New York–), 
numerals (_3–5_), dates (_Jan–Oct_), quantities, etc.
This function is represented in Metanorma i18n files as `punct.en-dash`.

===== Range of semantic functions

* The English nested paired quotation delimiters are often conflated with the apostrophe.
* Latin script conflates the punctuation marks for verbal and numeric ranges. CJK uses distinct punctuation marks for
verbal and numeric ranges. The latter function is represented in Metanorma i18n files as `punct.numeric-en-dash`.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: In good typography, EN DASH '<–>` (U+2013) is used. Informal use typically uses the hyphen.
* Traditional Chinese, Simplified Chinese, Japanese, Korean: EN DASH '<–>` (U+2013), WAVE DASH `<〜>` (U+301C),
FULLWIDTH TILDE `<～>` (U+FF5E)

===== Spacing rules

* Typically in Latin script there is no spacing around the en-dash. 
This differentiates it from the phrase stop
use of the en-dash.

===== Special handling

* In CJK, verbal range is conveyed through en-dash. Tilde and single em-dash can also be used for verbal range.
* Numeric range is normally wave dash.
Some Japanese academic writing uses colons instead of wave dash, though this is not universal.


// Name component separation:::
// Marks that separate parts of compound names or terms.

// Word division:::
// Marks that divide words at line breaks or indicate compound structures.

// ==== Language implementations

// [cols="2,2,2,2,2,2"]
// |===
// |Function |English |Traditional Chinese |Simplified Chinese |Japanese |Korean

// |Range indication +
// 破折號/破折号/ダッシュ/줄표
// |-/–/— +
// (hyphen/en dash/em dash)
// |—/～ +
// (破折號/波浪號)
// |—/～ +
// (破折号/波浪号)
// |～/ー +
// (波ダッシュ/長音符)
// |—/～ +
// (줄표/물결표)

// |Name separation +
// 間隔號/间隔号/中点/가운뎃점
// |·/- +
// (middle dot/hyphen)
// |· +
// (間隔號)
// |· +
// (间隔号)
// |・/･ +
// (中点/なかてん)
// |· +
// (가운뎃점)

// |Word division +
// 連字號/连字号/ハイフン/하이픈
// |- +
// (hyphen)
// |N/A
// |N/A
// |N/A
// |N/A
// |===

[[name-separator]]
==== Name separator

===== Primary function and purpose

Particularly in CJK, components of a name are separated from each other using a punctuation mark where 
necessary for clarity.
This applies to names in the broadest sense, including personal names (particularly foreign names),
professional titles and positions next to names, and
names of pieces of writing (i.e. titles, separating the title from the subtitle).

[example]
Simplified Chinese: 李奧納多·達·文西 "Leonardo·da·Vinci"

[example]
Japanese: 部長補佐・鈴木 (Assistant Department Head·Suzuki)

===== Range of semantic functions
* There is a continuity of function between the name separator and the word divider in CJK,
particularly as CJK uses ideographs for words (so words are not consistently broken down), 
and uses word dividers only sparingly; name separators then become the primary instance of
* The following are the more refined classes of name separator; because of the fluidity of classes, 
they are enumerated here rather than being broken down into separate
functions in the framework.
** Components of foreign names (Chinese, Japanese)
** Title and subtitle of a work
** Professional title, professional position, personal name
** Hierarchical components of cross-reference to entity in a document (e.g. _Clause 5, Note 1_).
In Metanorma Presentation XML, this is notated as `<span class="fmt-comma">`.



===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: In Western scripts, this function is conflated, with the normal word delimiter
(space), with phrase stops (comma by default, colon for title/subtitle), or with word divider
(hyphen, used in personal names).
** The colon or em-dash is used for title/subtitle: _Star Trek V: The Final Frontier_
** The hyphen is used within a single component of a personal name, a surname or a given name, 
e.g. _Jean-Jacques_, _Zeta-Jones_ 
* CJK: Various forms of interpunct: MIDDLE DOT `<·>` (U+OOB7) in China, HYPHENATION POINT `<‧>` (U+2027) in Taiwan,
KATAKANA MIDDLE DOT `<・>` (U+30FB) to separate Katakana words in Japanese.
** Japanese also uses WAVE DASH `<〜>` (U+301C), to separating title from subtitle
** Japanese also uses the KATAKANA-HIRAGANA DOUBLE HYPHEN `<゠>` (U+U+30A0) and the FULLWIDTH EQUALS SIGN
`<＝>` <U+FF1D>. This double hyphen is used to render Latin hyphen in transliteration of foreign names, i.e.
in the function of separating single components of a Western name. That function occasionally uses the interpunct instead.
More rarely, the double hyphen also is used instead of the interpunct to delimit names and titles in Western names, 
e.g. サー゠アーサー゠コナン゠ドイル "Sir=Arthur=Conan=Doyle"
** Japanese occasionally uses the interpunct for Japanese names, particularly when there would otherwise be 
confusion as to where one name ends and another begins.

===== Special handling
* The interpunct is half-width in Chinese in print, but full-width in online material.




==== Word separator (_space_)

===== Primary function and purpose

The word separator is used to separate words from each other.

===== Punctuation mark in scripts, languages, and locales

* In Latin and Cyrillic, this function is fulfilled by the space, which is not regarded as punctuation at all. The space is
applied universally as a word separator. 
  * This was not the case in antiquity. Roman inscriptions used an interpunct as a word separator; Greek and Roman manuscripts
  did not use word separators. (The latter practice is known as _scriptio continua_, "continuous writing")
* CJK by contrast defaults to not using word separators, outside of special contexts (here considered as name separators).
CJK therefore has _scriptio continua_.
* There are circumstances in which a word separator is necessary in careful text in CJK.
Japanese specifically uses the interpunct to separate ordinary Japanese words, outside of the special cases of 
name separators (<<name_separator>>), where the intended meaning would be unclear if the characters were written side-by-side.
It is more commonly used to separate foreign words and names when written in kana: パーソナル・コンピューター (personal computer). 




==== Word divider

===== Primary function and purpose

The word divider indicates the compound structure of a word, dividing it into pieces smaller than a word.

===== Range of semantic functions
* The word divider differs from the name separator in scope: name dividers operate on groupings of words, the word divider
operates within a word. 
* The hyphen is a special case of the word divider.
* The divisions that a word divider makes can vary in scope. What is divided up may be a compound consisting of meaningful units: 
independent words, or morphemes
(e.g. prefixes: _de-emphasise_). Or, what is divided up may be a single meaningful unit, broken up into phonetic units (syllables, or letters:
syllabification or spelling out of a word).
* The extent to which the word divider isused in compound words varies by language and by style. The use of hyphen to indicate
compound words (e.g. _pigeon-hole_, _craftily-constructed chair_) is on the decline in English.
* There may be two levels of division represented, with different dividers used to differentiate between them.

.Meaningful units
[example]
post-war +
Indo-European

.Phonetic units
[example]
R-E-S-P-E-C-T (spelling out) +
in-cre-di-ble (syllabification)

.Two levels of division
[example]
Pre–Indo-European


===== Punctuation mark in scripts, languages, and locales

* In Latin and Cyrillic, the default word divider is the hyphen, HYPHEN-MINUS `<->` (U+002D).
Unicode also has HYPHEN `<‐>` (U+2010) to disambiguate it from the minus sign, but "Unicode hyphen"
is little used, and fine typography prefers to use the distinct MINUS SIGN `<−>` (U+2212) 
in mathematical use.
* The EN DASH `<–>` (U+2013) is used as a higher level of division (_Pre–Indo-European_,
_San Francisco–area residents_; the latter uses en dash because it joins a noun consisting of two words with a space, to a third word).
* In lexicography, interpunct is also used for syllabification.


===== Spacing rules

Space is not meant to appear after word dividers normally, as any word separators would contradict the intent 
of the word divider as showing divisions within a single word.


===== Special handling

There are cases of word divider which are not meant to be conflated with the hyphen, and for which a line break
between word divisions is inappropriate. For such cases, NON-BREAKING HYPHEN `<‑>` (U+2011) is used.


==== Hyphen
===== Primary function and purpose

The hyphen is a special case of Word Divider. It is used to divide a word at a line-break,
so that the amount of space in a line is reduced.

===== Punctuation mark in scripts, languages, and locales

* The hyphen is in routine use in Latin and Cyrillic.
* The word delimiter is alien to CJK; accordingly, the hyphen is alien to CJK.

===== Spacing rules

The hyphen is only meaningful in that function at the end of a line, and no characters are meant to appear
after it.

===== Special handling

* An optional hyphen, SOFT HYPHEN (U+00AD) is often used in word processing, which is only rendered when near a line break.


==== Identifier divider

===== Primary function and purpose

The identifier divider is used to break up a token that is not a linguistic expression.

[example]
====
Phone number: _555-8787_ +
Date: _2000-01-01 +
Document identifier: __ISO 639-2__
====

===== Range of semantic functions
* The function of the identifier dividier and the word divider are quite similar: they both are breaking up a token and showing
its internal structure, but differ in that one divides a linguistic word, and the other divides a non-linguistic token.
* The function of the identifier dividier is also similar to the decimal point, as a number token divider; however
decimal points are dealt with separetely, as numeric formatting.
* Identifier dividers can apply to numbers as identifiers, but are distinct from decimal points in that they don't have an arithmetic
function. So when separating groups of digits in a phone number or a date, the hyphen is distinct from a decimal point.
* The components of a numeric date are also split by identifier divider.
* In the case of hierarchical identifiers of entities in a document, such as clauses,
Metanorma Presentation XML notates this as `<span class="fmt-autonum-delim">`.

===== Punctuation mark in scripts, languages, and locales

* The usual identifier divider is the hyphen. Period, slash, and colon are also used idiosyncratically.

==== Identifier delimiter

===== Primary function and purpose

The identifier divider is used to separate an identifier from surrounding text, whether at the start,
the end, or both.

[example]
====
Formula (1) +
Figure 1 a)
====

===== Range of semantic functions
* Identifier delimiters distinct from word delimiters are very rare. The most common use of them
is in structured documents such as under Metanorma, for subfigures and formulas. Whether an
identifier delimiter is used is a matter of style convention.
* The identifier delimiter is similar in function to title marks and emphasis marks.
* Metanorma Presentation XML currently notates this function as `<span class="fmt-autonum-delim">`,
although this is properly the notation for identifier dividers.

===== Punctuation mark in scripts, languages, and locales

* The usual identifier delimiter is the close parenthesis, or surrounding parentheses.





=== Caption markers
==== General

===== Primary function and purpose

In formal documents (particularly the standards in scope of Metanorma), titles of cross-referencable entities
within the document (figures, tables, lists, list items, clauses, etc.) are labelled in such a way as to
isolate the identifier of the entity, which is used in cross-referencing

==== Caption number delimiter
===== Primary function and purpose


The identifier of the entity may be signalled with punctuation acting as a delimiter

[example]
Table 1.

[example]
3)

===== Range of semantic functions
* Different kinds of entity can take different punctuation. Ordered list items are commonly delimited with a closing
parenthesis instead of a period.

===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: Period is the most common caption number delimiter, if a delimiter is used at all.
Closing parentheses are common for ordered list items.

==== Caption number separator


===== Primary function and purpose

If the number of the entity appears together with a caption or title, giving more information about the entity, 
puncutation is usually used to separate the two. 
In Metanorma Presentation XML, this is notated as `<span class="fmt-caption-delim">`

[example]
Table 1. Distribution of rice yields

[example]
2.1: Soil erosion 


===== Range of semantic functions
* Different kinds of entity can take different punctuation. For example, a document may use period as a caption
separator for clauses, but dash for tables and figures.
* This function overlaps with the caption number delimiter. If a caption number delimiter is used, a distinct
separator is usually not used after it. The following illustrates different possible configurations.
+
.No caption number delimiter, colon as caption number separator
====
Table 1
Table 1: Distribution of rice yields
====
+
.No caption number delimiter, period as caption number separator
====
Table 1
Table 1. Distribution of rice yields
====
+
.Period as caption number delimiter, caption delimiter blocks distinct caption number separator
====
Table 1.
Table 1. Distribution of rice yields
NOT: Table 1.: Distribution of rice yields
====


===== Punctuation mark in scripts, languages, and locales

* Latin, Cyrillic: All of the following may appear in this function: space (so no special punctuation),
colon, comma, em-dash, period


==== Caption stop


===== Primary function and purpose

The caption or title of an entity in a document may be terminated with a punctuation mark. 
In Metanorma Presentation XML, this is notated as `<span class="fmt-label-delim">`

===== Range of semantic functions

* This function is an extension of the sentence stop, since captions can be seen as
sentences. Whether it is used or not depends on the publisher's style conventions.















== Functions: adding meaning

=== Annotation markers

==== General

===== Primary function and purpose

Text annotation markers provide supplementary information or references
related to the main text content.


===== Range of semantic functions

* Annotation markers are routinely conflated with grouping markers, which enclose, separate, or highlight 
specific text elements.


Japanese: In pairs, in place of dashes or brackets: 〜〜答え〜〜


* The Leiden conventions for classical inscriptions and papyri use a wide range of brackets with quite distinct meanings,
in order to convey editorial approaches to a text. (They are a form of machine readable text changes.) 
This is the upper limit of distinct semantic functions in annotation
markers, and most use of annotation markers is semantically much more ad hoc:
** ạḅ: letter is unclear in original
** [abc]: letters missing from original (because text is broken off), restored by editor
** ⟨abc⟩: letters left out from original text (because the scribe never wrote them), restored by editor
** a(bc): abbreviation in original, letters expanded by editor
** {abc}: letters written in the original by the scribe, deleted as errors by the editor
** ⟦abc⟧: letters deleted in the original by the scribe, restored by the editor
** \abc/: letters interpolated in the original by the scribe (typically between lines)


==== Parenthetical annotation

===== Primary function and purpose

This function is represented in Metanorma i18n files as `punct.open-paren` and `punct.close-paren`.

// Parenthetical information:::
// Marks that enclose supplementary or explanatory information.


[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|parenthetical annotation
|(...)
| This is a sentence (with additional information).

|Traditional Chinese
|夾註
|（...）
| 這是一句句子（帶有附加信息）。

|Simplified Chinese
|夹注
|（...）
| 这是一句句子（带有附加信息）。

|Japanese
|括弧注
|（...）
| これは文です（追加情報付き）。

|Korean
|괄호주
|(...)
| 이것은 문장입니다 (추가 정보 포함).

|===










==== Footnote/superscript annotation

Text annotation marks that provide supplementary information or references
related to the main text content, typically placed at the bottom of the page or
as superscript.

[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|footnotes/superscript
|footnotes/superscript
| This is a sentence with a footnote.

|Traditional Chinese
|傍註
| ?
| 這是一句帶有傍註的句子。

|Simplified Chinese
|旁注
| ?
| 这是一句带有旁注的句子。

|Japanese
|傍注
| ?
| これは傍注付きの文です。

|Korean
|방주
|footnotes/superscript
| 이것은 각주가 있는 문장입니다.

|===

// Annotation emphasis:::
// Marks that add supplementary information above or alongside text.

// ==== Language implementations

// [cols="2,2,2,2,2,2"]
// |===
// |Function |English |Traditional Chinese |Simplified Chinese |Japanese |Korean

// |Stress emphasis +
// 着重號/着重号/圏点/방점
// |*bold* or _italic_ +
// (emphasis)
// |‥‥‥ +
// (着重號)
// |‥‥‥ +
// (着重号)
// |‥‥‥ +
// (圏点/けんてん)
// |*bold* or _italic_ +
// (방점)

// |Proper name marking +
// 專名號/专名号/固有名詞線/고유명사표
// |N/A +
// (not used)
// |人̲名̲ +
// (專名號)
// |人̲名̲ +
// (专名号)
// |人̲名̲ +
// (固有名詞線)
// |N/A +
// (not used)

// |Annotation emphasis +
// 旁註點/旁注点/傍注/방주
// |footnotes/superscript +
// (annotation marks)
// |側點 +
// (旁註點)
// |侧点 +
// (旁注点)
// |傍点 +
// (傍注)
// |footnotes/superscript +
// (방주)
// |===

=== Grouping markers

==== Bracket

==== Brace

==== Angle bracket




=== Semantic identification

==== General

Traditional marks that identify names and titles in classical texts.

NN: Title marks are to be handled in relaton-render as bibliographic punctuation, not in general l10n translation.

NN: Traditional Chinese uses wavy underline U+FE4F as official title markup. Square brackets 【】and double quotation marks『』are also used. Single title marks 〈...〉are used for article and chapter titles.

NN: Simplified Chinese uses《...》for titles, with inner marks 〈...〉for article and chapter titles. Square brackets【】and double quotation marks『』are also used for song titles.

NN: Japanese uses『...』(double quotes) for book titles.

NN: Western quotes are unofficially used in press for titles in Traditional Chinese, Japanese, and Korean.

NN: Double quotes are also used for book titles.

Apostrophe markers

Number markers




==== Title mark


// Title marking:::
// Specialized marks for book titles, article titles, and similar works.

Japanese: To mark subtitles: 〜概要〜



[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|N/A
|N/A
|N/A

|Traditional Chinese
|書名號
|`﹏...﹏` or `《...》`
|我讀過《紅樓夢》。

|Simplified Chinese
|书名号
|`《...》`, inner mark `〈...〉`
|我读过《红楼梦》。

|Japanese
|鉤括弧
|`『...』`
|私は『源氏物語』を読みました。

|Korean
|책이름표
|`『...』`
|나는 『홍루몽』을 읽었습니다。

|===

// |Title marking +
// 書名號/书名号/書名符號/책이름표
// |"..." or _..._ +
// (quotation/italics)
// |﹏...﹏/《...》 +
// (書名號)
// |《...》 +
// (书名号)
// |『...』 +
// (鉤括弧)
// |『...』 +
// (책이름표)


==== Title delimiter

[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|N/A
|N/A
|N/A

|Traditional Chinese
|書章分隔號
|`．`
|《老子．道德經》

|Simplified Chinese
|书章分隔号
|`．`
|《老子．道德经》

|Japanese
|書章分隔符
|`．`
|《老子．道徳経》

|Korean
|책장구분
|`．`
|『노자．도덕경』

|===



==== Proper name mark

[cols="2,2,2,3"]
|===
|Language |Name of Mark |Symbol |Example

|English
|N/A
|N/A
|N/A

|Traditional Chinese
|專名號
|`人̲名̲`
|我讀過《紅樓夢》，作者是曹雪芹。

|Simplified Chinese
|专名号
|`人̲名̲`
|我读过《红楼梦》，作者是曹雪芹。

|Japanese
|固有名詞線
|`人̲名̲`
|私は『紅楼夢』を読みました。著者は曹雪芹です。

|Korean
|고유명사표
|?
|?

|===



=== Emphasis markers

==== General

===== Primary function and purpose

Emphasis markers highlight and draw attention to specific text elements through visual modification or
annotation, for a range of functions.

===== Range of semantic functions

* The range of functions for which emphasis markers are used is open-ended, and often overlaps with other
semantic functions. This includes emphasis proper,  on the part of the author; emphasis on the part of a speaker
in direct speech; use-mention distinction (so citation of words as subjects of
discussion, as opposed to use of words in language); titles and names of entities; and foreign words.
* The extent of use of emphasis markers varies by language and style.

===== Punctuation mark in scripts, languages, and locales

* Latin and Cyrillic do not use punctuation in this function, and resort instead to typographical styling,
including italics, boldface, and underlining. These are not in scope of this framework, except where Metanorma
needs to alternate between typographical styling in Latin/Cyrillic, and punctuation marks in CJK. 
  * While some documents make meaningful distinctions between italics, boldface, and underlining, they are
idiosyncratic to the document, and cannot be generalised readily. Italics is the default emphasis marking device in
English.
* CJK does not use italics, although it does use boldface.
  * Instead of italics, CJK uses a range of explicit punctuation, including single or double quotes, brackets,
or emphasis marks.


==== Emphasis marks


===== Punctuation mark in scripts, languages, and locales

* CJK emphasis marks are a diacritic applied to every character in the text span to be emphasised. 
Chinese is usually restricted to using an underdot. Japanese uses a much wider range of emphasis marks:
filled or hollow dot, circle, double circle, triangle, and "sesame" marks.

===== Special handling

* While the underdot emphasis mark could be realised as a distinct Unicode character, CJK emphasis marks have been
treated in computer typesetting as markup. They are realised in CSS using the `text-emphasis-style` attribute.
This means that Western and CJK emphasis marks are both treated as markup rather than as punctuation.
* CJK emphasis marks are rare online and unsupported in many Word processors. (Microsoft Word also supports a
smaller range of emphasis marks in Japanese than does HTML CSS or XSL:FO.)

[TIP]
--
Metanorma does not currently support emphasis marks. When it does, it will be as a discretionary expansion
of `<em>` markup in Semantic Metanorma XML.
--



== Functions: other

=== Numeric punctuation

==== General

Punctuation used to convey different types of number overlaps with other punctuation discussed here, but it not
in scope of this discussion, and Metanorma handles it with distinct mechanisms. It is briefly outlined here for
completeness, and to identify potential points of ambiguity.

The punctuation used to convey different types of number also overlaps with some  symbols for mathematical functions
(notably plus and minus signs); mathematical functions are out of scope.


The semantic functions

==== Decimal point (_decimal point_)




NN: Simplified Chinese often uses `*` for multiplication instead of traditional symbols.

NN: The Japanese interpunct (Katakana middle dot `・`) is used for decimal points in Chinese numbers. 
For example, "3.14" becomes "三・一四".

NN: In Traditional Chinese, U+2027 is used for decimal point in Chinese numbers. For example, "3.5" becomes "三‧五".





=== Miscellaneous


This function is represented in Metanorma i18n files as `punct.ellipse`.

NN: Ellipsis in CJK languages can be three dots or six dots. In horizontal text, it can be baseline or centered. In vertical text, it is centered. The six-dot form uses twice U+22EF as explicit centered markup, or twice U+2026 where centering is inexplicit.





== Whitespace handling

NN: CJK full-width space (U+3000) is rare and meaningful. It is used in specific contexts, for example as an honorific spacing for certain names (e.g., Chang Kai-shek in Chinese text).

NN: Japanese requires a quarter-em space (U+2005) between Japanese text and Latin characters. This applies to the transition between Japanese characters and any Latin text.

NN: There is an open question about whether the quarter-em space rule should apply between Japanese and numbers. Currently implemented to apply between Japanese and all Latin characters including numbers.

NN: Japanese also requires space after non-Japanese punctuation marks, such as exclamation marks and question marks.

NN: In Japanese, a single space is used at the start of paragraphs in text with little Kanji for disambiguation purposes.

NN: Japanese can use U+3000 (full-width space) in text with little Kanji for better readability and disambiguation.


This function is represented in Metanorma i18n files as `punct.cjk-latin-separator`.

== Metanorma Implementation

=== Three classes of punctuation translation

NN: There are three classes of punctuation translation/localisation in the Metanorma system:

add smart quotes. Add number marks

==== Bibliographic punctuation

NN: Bibliographic punctuation is distinct from auto-text punctuation and is already catered for in the relaton-render gem. This class of punctuation does not need to be considered in isodoc-i18n, as it will be delegated to that gem.

NN: Title marks and subtitle delimiters (such as the Japanese use of wave dash) are to be handled in relaton-render as bibliographic punctuation.

This is going to change 

==== Auto-text punctuation via l10n()

NN: Auto-text punctuation translation through the l10n() function will continue to be supported. This mechanism translates Latin punctuation in automatically generated text to the appropriate target language punctuation.

NN: The input to l10n() will continue to be Latin punctuation where possible, but some distinctions are not possible with Latin as a starting point.

NN: Auto-text is restricted in what it should be called on to represent; ellipses are supported but not anticipated in common use.

This is going to change

==== Direct i18n config values

NN: Building text directly from i18n configuration values is also supported. It is not yet clear whether this will replace l10n() entirely, but it won't in the immediate term.

* Integration with auto-numbering systems

Markup of delimiter types

=== Configurable punctuation marks

NN: Most punctuation has variability and needs to be configurable. All punctuation will be configured in the i18n YAML files.

NN: One configuration value for punctuation per document will be used. The system cannot flip between different punctuation styles within a language and a document.

NN: Punctuation configuration will be set in i18n YAML, which will be per language in isodoc-i18n, overridable per flavour/language, and then overridable again per document.

NN: The following is the set of configurable punctuation marks in isodoc-i18n:

* colon
* comma
* enum-comma
* semicolon
* period
* close-paren
* open-paren
* close-bracket
* open-bracket
* question-mark
* exclamation-mark
* emphasis-mark
* em-dash
* en-dash
* number-en-dash
* open-quote
* close-quote
* open-nested-quote
* close-nested-quote
* ellipse

NN: A subset of the foregoing will be translated from Roman punctuation in autotext via l10n():

* colon: `:`
* comma: `,`
* enum-comma (contextual)
* semicolon: `;`
* period: `.`
* close-paren: `)`
* open-paren: `(`
* close-bracket: `]`
* open-bracket: `[`
* question-mark: `?`
* exclamation-mark: `!`
* emphasis-mark (contextual)
* em-dash: `—`
* en-dash: `–`
* number-en-dash: `–` (invoked depending on context - both surrounding characters need to be Unicode numbers)
* open-quote: `"`
* close-quote: `"`
* open-nested-quote: `'`
* close-nested-quote: `'`
* ellipse: `…`

NN: Add the relaton-render q-mark qq-mark, and move it to punct

NN: Enum comma is not to be supported in l10n() directly. It is already handled with explicit support through the `multiple_and` i18n YAML value.

NN: The input to l10n() needs to be unambiguous. In particular, the default quotation uses `" "`. Nested quotes use `' '` in templated auto-text, reserving `'` for apostrophe. Ellipse uses the single character `…`.

=== Self-references in YAML configuration

NN: Self-references in the YAML are realized as `#{ self.label }`, for example referencing the current enum-comma:

[source,yaml]
----
#{ self["punct"]["enum-comma"] }
----

NN: Liquid syntax is not used because Liquid is already being used to pass external variables into i18n, and Liquid needs to be unprocessed in such i18n entries.

=== Writing mode parameterization

NN: Script directionality has no standard code comparable to ISO 15924. The online assumption has been to use the CSS `writing-mode` style to convey directionality, as it is considered a presentation concern.

NN: RFC 5646 combines language, script, and locale, none of which are related to directionality. It is further extensible, but the IANA Registry does not cater for script directionality.

NN: A fourth optional parameter is added to l10n(), alongside locale, for writing_mode: `vertical-rl`, `vertical-lr`, `horizontal-tb`.

NN: This allows differentiation of punctuation based on text direction, which is necessary for some CJK languages where punctuation changes between horizontal and vertical text layouts.

[bibliography]
== Bibliography

* [[[re_PunctuationMarks,GB/T 15834-2011]]]
// GB/T 15834-2011. _Rules for punctuation usage in Chinese writing_. Standards Press of China. 2011.

* [[[tw_punct,1]]] Taiwan Ministry of Education. _Revised Handbook of Punctuation_ (Second Edition). Ministry of Education. 2017.

* [[[bunka_guidelines,1]]] Japan Cultural Affairs Agency. _Guidelines for Creating Public Documents (Cultural Affairs Council Recommendation)_. Cultural Affairs Council. 2022.

* [[[kumihan-en-2,4]]] Japan Electronic Publishing Association. _Requirements for Japanese Text Layout_. W3C Working Group Note. 2012.
